import { GlyStd } from "@gamely/gly-types";

export class RenderSystem {
  initialize(std: GlyStd) {
    // Initialize rendering system, set up canvas, etc.
    console.log("RenderSystem initialized");
  }
}
